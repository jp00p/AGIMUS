apiVersion: v1
kind: Service
metadata:
  name: mysql-service
spec:
  ports:
  - port: 3306
  selector:
    app: mysql
  clusterIP: None
  # type: NodePort
  # selector:
  #   app: mysql
  # ports:
  #   - protocol: TCP
  #     port: 3306
  #     targetPort: 3306
  #     nodePort: 30006